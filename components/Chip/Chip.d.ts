import { ChipProps } from './types';
export declare const Chip: ({ type, size, state, disabled, active, text, children, leadingIcon, trailingIcon, onClick, className, radius, lang, }: ChipProps) => import("react/jsx-runtime").JSX.Element;
